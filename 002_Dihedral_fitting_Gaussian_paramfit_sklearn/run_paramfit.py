import MDtraj as md
import sklearn.metrics as sk1
import parmed
import get_

list_pdb
t = md.load('trajectory.h5', top=)

join(other, check_topology=True, discard_overlapping_frames=False)
save_mdcrd

for bond in bonds
	traj = md.load(pdb1, top=   )
	for pdb in os.listdir():
		traj.join(other, check_topology=True, discard_overlapping_frames=False)

	traj.n_frames
	traj.save_mdcrd('')

	QM_data=
	singlepointenergy=

	#return K
	paramfit_fit_K=

	#paramfit

	write paramfit_job_list

	parmed.load.prmtop
	parmed.deleteDihedral<mask1> <mask2> <mask3> <mask4>
	parmed.addDihedral<mask1> <mask2> <mask3> <mask4> <phi_k> <per> <phase> <scee> <scnb>
	singlepointenergyparamfit=

	score_before= sk1.r2_score(QM_data, singlepointenergy)
	score_after =  sk1.r2_score(QM_data, singlepointenergyparamfit )
